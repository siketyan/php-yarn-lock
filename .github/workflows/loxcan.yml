name: LoXcan

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: siketyan/loxcan@v0.7.0
